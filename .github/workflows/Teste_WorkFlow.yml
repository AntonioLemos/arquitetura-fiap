
name: Teste WorkFlow


on:
  push:
    branches: [ "main", "developer" ]



jobs:
  test_job:
    name: Executanto job de teste
    runs-on: self-hosted
    steps:
    - name: Step 1
      uses: actions/checkout@v2
      
    - name: Docker Login
      uses: docker/login-action@v3.1.0
      with:   
        username: 'lemos.antoniof@gmail.com'
        password: ${{ secrets.DOCKER_LOGIN }}

          



